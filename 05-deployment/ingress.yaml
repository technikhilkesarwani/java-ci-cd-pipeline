apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: java-cicd-demo-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: java-cicd-demo.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: java-cicd-demo-service
                port:
                  number: 80